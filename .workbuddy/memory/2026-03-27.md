# 2026-03-27 工作日志

## 文章功能开发完成 ✅
- 创建 `test/create_articles_table.sql`：建表 + RLS + 3条示例数据
- 创建 `test/insert_test_articles.sql`：4条测试文章（含带/不带封面图的各类分类）
- 创建 `test/grant_select_to_anon.sql`：修复 anon 角色 SELECT 权限问题
- petmind PC端新增 `/admin/articles` 文章列表页（密钥认证、搜索筛选、发布/下架/删除）
- petmind PC端新增 `/admin/articles/edit` 编辑页（富文本工具栏、实时预览、封面图）
- petmind-mini 首页今日动态改为从 articles 表拉取真实数据（支持骨架屏、空态）
- petmind-mini 新增文章详情页 `/pages/article/article`（封面图 + 富文本 rich-text 渲染）
- 首页文章卡片样式优化：垂直布局、全宽封面图、移除箭头
- 构建验证通过

## 管理后台 UI 全面重构
- 全新设计风格：纯色扁平 + 简洁现代
- 左侧菜单：白色背景，深灰色选中状态，支持收起/展开
- 登录页：纯白卡片 + 灰色背景，简洁干净
- 首页：白色统计卡片 + 彩色图标，用户列表优化
- 文章管理：白色卡片 + 简洁表格，状态标签带圆点
- 文章编辑：分类选择卡片网格，状态选择大卡片形式
- 统一圆角风格：xl (12px) 适中圆角，更专业

## 其他修复
- 保存日程按钮文字居中（添加 flex + align-items + justify-content）
- 日历页 401 权限问题：需在 Supabase 执行 `test/fix_events_final.sql`
- 保存日程无效：移除 event_type 字段（数据库表中不存在该字段）
- 全站返回按钮样式重设计（毛玻璃效果 + 点击缩放动效，统一所有页面）
